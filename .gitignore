# Dependencies
node_modules

# Next.js build output
.next

# Environment variables — never commit secrets
.env
.env.local

# TypeScript build info
*.tsbuildinfo

# OS metadata
.DS_Store
**/.DS_Store
Thumbs.db

# Claude Code workspace
.claude

# Design import assets (large source files, not needed in prod)
_design-import

# Claude Code project instructions — auto-loaded from root, local only
ISKOARENA_CLAUDE.md
test-supabase.ts

# All internal team docs, task briefs, and debug session reports — local only
internal/
reports/
